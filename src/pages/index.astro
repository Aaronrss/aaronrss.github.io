---
import Card from "@/components/Card.astro";
import Hero from "@/components/Hero.astro";
import SectionHeader from "@/components/SectionHeader.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const allPosts = await getCollection("blog", ({ data }) => !data.draft);
const recentPosts = allPosts
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

const allProjects = await getCollection("portfolio");
const featuredProjects = allProjects
  .filter((p) => p.data.featured)
  .sort((a, b) => a.data.order - b.data.order)
  .slice(0, 3);
---

<BaseLayout title="Portfolio | Web Developer">
  <Hero
    title="Hello, my name is Aaron Rosas"
    subtitle="Welcome to my portfolio"
    description="Software Engineer based in CDMX, México."
    showCTA={true}
    ctaText="View Portfolio"
    ctaLink="/portfolio"
    secondaryCTA="Read Blog"
    secondaryCTALink="/blog"
  />

  <section class="py-20 md:py-12">
    <div class="container">
      <SectionHeader
        title="Last Articles"
        subtitle="Thoughts, tutorials, and learnings about web development"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          recentPosts.map((post) => (
            <Card
              title={post.data.title}
              description={post.data.description}
              href={`/blog/${post.slug}`}
              image={post.data.image}
              date={post.data.pubDate}
              category={post.data.category}
              tags={post.data.tags}
              type="blog"
            />
          ))
        }
      </div>

      <div class="text-center mt-12">
        <a href="/blog" class="btn btn-secondary">View all articles</a>
      </div>
    </div>
  </section>

  <section class="py-20 md:py-12 bg-[var(--bg-secondary)]">
    <div class="container">
      <SectionHeader
        title="Featured Projects"
        subtitle="A selection of my most recent work"
      />

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {
          featuredProjects.map((project) => (
            <Card
              title={project.data.title}
              description={project.data.description}
              image={project.data.image}
              technologies={project.data.technologies}
              github={project.data.github}
              demo={project.data.demo}
              type="portfolio"
            />
          ))
        }
      </div>

      <div class="text-center mt-12">
        <a href="/portfolio" class="btn btn-secondary">View all projects</a>
      </div>
    </div>
  </section>
</BaseLayout>
