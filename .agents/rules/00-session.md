# Session rules (AGY / readers of .agents/rules)

- Read repository `AGENTS.md` before large refactors.
- Prefer small diffs and existing patterns over new frameworks.
- No secrets in commits or command output.
- Mission skills live under `.agents/skills/`; do not invent global installs without asking.
